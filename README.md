# Simple-Chat
A simple chat application using the linux sockets

## Requirements
- x86_64 linux
- C++23
- CMake 3.16

## Build
1. Create build directory
```bash
mkdir build && cd build
```

2. Build the project
```bash
cmake .. && cmake --build .
```

3. run
```bash
./server
./client
```
